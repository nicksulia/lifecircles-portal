import React, { PureComponent } from 'react';

class SearchBar extends PureComponent {
    render() {
        return (
            <div>
                Search Bar
            </div>
        );
    }
}

export default SearchBar;